language: python

matrix:
  include:
  - os: linux
    dist: trusty
    sudo: false
    python: '2.7'
python:
  - "2.7"
install:
  - pip install -r requirements.txt
script:
  - tar -zcvf myapp.tgz myapp
  - python bin/helloworld.py -p $splunk_pw
on:
    branch: master
