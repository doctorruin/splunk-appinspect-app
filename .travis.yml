language: python

matrix:
  include:
  - os: linux
    dist: trusty
    sudo: false
    python: '2.7'
python:
  - "2.7"
before_install:
  - ls
  - tar -C myapp -zcvf myapp.tgz *
install:
  - pip install -r requirements.txt
script:
  - python bin/helloworld.py -p $splunk_pw
on:
    branch: master
